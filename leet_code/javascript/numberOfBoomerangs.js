function numberOfBoomerangs(points) {
//  find all unique combinations of the points;
//  iterate through each combination, check if all pairs have the same x or y axis;
//  check to see if the x or y (the point that isn't the same), matches the distance to the first coordinate (same distance apart)
  let count = 0;

  

}

let points = [
  [0, 0],
  [1, 0],
  [2, 0],
];

console.log(numberOfBoomerangs(points));
